import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import MobileSlides from "@/LandingPages/Brand/Components/MobileSlides/MobileSlides";

<Meta
  title="Components/MobileSlides"
  component={MobileSlides}
/>

# MobileSlides

MobileSlides is a section that provides a blurb about subscriptions on the Brand landing page

export const Template = (args) => <MobileSlides {...args} />;

### Playground

Experiment with `MobileSlides` via the Controls addon in the Canvas tab.

<Canvas>
  <Story
    name="Default"
  >
    {Template.bind({})}
  </Story>
</Canvas>


<ArgsTable story="Default" />